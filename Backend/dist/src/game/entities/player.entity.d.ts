export declare class Player {
    socketId: string;
    playerNo: number;
    score: number;
    x: number;
    y: number;
    playerId: number;
    playerName: string;
    playerImg: string;
    constructor(socketId: string, playerNo: number, score: number, x: number, y: number, playerId: number, playerName: string, playerImg: string);
}
